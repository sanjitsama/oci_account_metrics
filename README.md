# Shell Script/CLI Project enabling users to collect and process key metrics on their cloud tenancies via scheduled runs

## Steps to Run:
- Create virtualenv: `virtualenv venv`
- Begin using the virtual environment: `source venv/bin/activate`
- Install python dependencies: `pip install -r requirements.txt`
- Run main project script (from `~/oci_metrics`): `python `